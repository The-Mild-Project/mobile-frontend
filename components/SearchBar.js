import React, { useState } from 'react';
import { FlatList, Image, Platform, StyleSheet, Text, TouchableOpacity, View, Button, AsyncStorage, Picker } from 'react-native';

const SearchBar = ({term, onTermChange}) => {

}